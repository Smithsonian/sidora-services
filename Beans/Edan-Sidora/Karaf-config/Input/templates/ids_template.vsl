<?xml version="1.0" encoding="ISO-8859-1"?>
<Assets>
#foreach( $asset in ${headers.idsAssetList} )
  <Asset Name="${headers.idsAssetImagePrefix}${asset.imageid}.JPG" IsPublic="$asset.isPublic" IsInternal="$asset.isInternal" MaxSize="3000" InternalMaxSize="4000">${headers.idsAssetImagePrefix}$asset.imageid</Asset>
#end
</Assets>
