### This test requires a running FITS Servlet service.  Use this property to point to its location.
### If running from ServiceMix:
#Make sure the `si.fits.host` property is set in the `<smx-home>/etc/system.properties` property file
si.fits.host=http://localhost:9180/fits-1.1.3