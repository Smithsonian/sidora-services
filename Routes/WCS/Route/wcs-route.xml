<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Copyright 2015 Smithsonian Institution.
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License"); you may not
  ~ use this file except in compliance with the License.You may obtain a copy of
  ~ the License at: http://www.apache.org/licenses/
  ~
  ~ This software and accompanying documentation is supplied without
  ~ warranty of any kind. The copyright holder and the Smithsonian Institution:
  ~ (1) expressly disclaim any warranties, express or implied, including but not
  ~ limited to any implied warranties of merchantability, fitness for a
  ~ particular purpose, title or non-infringement; (2) do not assume any legal
  ~ liability or responsibility for the accuracy, completeness, or usefulness of
  ~ the software; (3) do not represent that use of the software would not
  ~ infringe privately owned rights; (4) do not warrant that the software
  ~ is error-free or will be maintained, supported, updated or enhanced;
  ~ (5) will not be liable for any indirect, incidental, consequential special
  ~ or punitive damages of any kind or nature, including but not limited to lost
  ~ profits or loss of data, on any basis arising from contract, tort or
  ~ otherwise, even if any of the parties has been warned of the possibility of
  ~ such loss or damage.
  ~
  ~ This distribution includes several third-party libraries, each with their own
  ~ license terms. For a complete copy of all copyright and license terms, including
  ~ those of third-party libraries, please see the product release notes.
  -->

<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
    xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd">

    <!-- OSGI blueprint property placeholder -->
    <!-- The properties should not be changed from karaf or external cfg if there are deployments currently being ingested
        as it would cause the camel context to reload and error out any in-flight ingestion processes. -->
    <cm:property-placeholder id="sidora.karaf.properties" persistent-id="edu.si.sidora.karaf" update-strategy="reload">
        <cm:default-properties>
            <cm:property name="si.ct.wcs.dryrun" value="false"/>
        </cm:default-properties>
    </cm:property-placeholder>

    <bean id="pidAggregator" class="edu.si.services.fedorarepo.aggregators.PidAggregationStrategy"/>

    <bean id="noRootObject" class="java.lang.IllegalArgumentException">
        <argument value="WCSIngest: Root object does not exist."/>
    </bean>

    <bean id="noImageSequence" class="java.lang.IllegalArgumentException">
        <argument value="WCSIngest: Image sequence does not exist."/>
    </bean>

    <bean id="cameraTrapRoutes" class="edu.si.services.beans.cameratrap.CameraTrapRouteBuilder"/>

    <bean id="velocityToolsHandler" class="edu.si.services.beans.velocityToolsHandler.VelocityToolsHandler"/>

    <bean id="deploymentPkgValidator" class="edu.si.services.beans.cameratrap.DeploymentPackageValidator"/>

    <bean id="cameraTrapStaticStore" class="edu.si.services.beans.cameratrap.CameraTrapStaticStore"/>
    <!-- errorHandlerRef="wcsingestErrorHandler" -->

    <!-- processsor used during fedora findObject redelivery attempts to check for in-flight process-->
    <bean id="inFlightCheckProcessor" class="edu.si.services.beans.cameratrap.InFlightCheckProcessor"/>

    <camelContext id="WCSIngestCamelContext"
                  xmlns="http://camel.apache.org/schema/blueprint"
                  xmlns:ri="http://www.w3.org/2001/sw/DataAccess/rf1/result"
                  xmlns:fits="http://hul.harvard.edu/ois/xml/ns/fits/fits_output"
                  xsi:schemaLocation="http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint-2.16.1.xsd"
                  trace="false">

        <!--
          The "unused" namespace definitions above are needed to deal with the way Camel handles namespaces in XPath.
        -->
        <!-- Using a Camel properties component and referring to the blueprint property placeholder by its ID. -->
        <propertyPlaceholder id="properties"
                             location="blueprint:sidora.karaf.properties,file:${karaf.home}/etc/system.properties"/>

        <!--
        <errorHandler id="wcsingestErrorHandler" type="DefaultErrorHandler">
            <redeliveryPolicy maximumRedeliveries="20"
                              retryAttemptedLogLevel="WARN"
                              backOffMultiplier="2"
                              useExponentialBackOff="true"/>
        </errorHandler>
        -->

        <errorHandler id="noErrorHandler" type="NoErrorHandler" />

        <routeBuilder ref="cameraTrapRoutes"/>

        <onException>
            <exception>java.net.ConnectException</exception>
            <redeliveryPolicy useExponentialBackOff="true"
                              backOffMultiplier="2"
                              redeliveryDelay="1000"
                              maximumRedeliveries="200"
                              retryAttemptedLogLevel="WARN"/>
        </onException>

        <onException>
            <exception>java.io.FileNotFoundException</exception>
            <redeliveryPolicy useExponentialBackOff="true"
                              backOffMultiplier="2"
                              redeliveryDelay="1000"
                              maximumRedeliveries="5"
                              retryAttemptedLogLevel="WARN"/>
        </onException>

        <onException useOriginalMessage="true" onRedeliveryRef="inFlightCheckProcessor">
            <exception>edu.si.services.fedorarepo.FedoraObjectNotFoundException</exception>
            <redeliveryPolicy useExponentialBackOff="true"
                              backOffMultiplier="2"
                              redeliveryDelay="1000"
                              maximumRedeliveries="7"
                              retryAttemptedLogLevel="WARN"
                              retriesExhaustedLogLevel="WARN"
                              logExhaustedMessageHistory="false"/>
            <continued>
                <constant>true</constant>
            </continued>
        </onException>

        <!--
        <onException id="thumbnailError">
            <exception>javax.imageio.IIOException</exception>
            <redeliveryPolicy maximumRedeliveries="0"/>
            <continued>
                <constant>
                    true
                </constant>
            </continued>
        </onException>
        -->

        <route id="WCSStartProcessing">
            <from uri="file:ProcessWCS?delay=9000&amp;moveFailed=Error-WCS&amp;delete=true&amp;maxMessagesPerPoll=1"/>
            <log message="${id} WCSIngest: Starting WCS processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Needed for the Velocity tools. -->
            <setHeader headerName="esc">
                <method ref="velocityToolsHandler" method="getVelocityEscapeTool"/>
            </setHeader>

            <!--If there is an issue with staging large archives we can can 'slow' down the route with delay below. -->
            <delay>
                <constant>6000</constant>
            </delay>

            <log message="${id} WCSIngest: Starting Extraction"/>
            <to uri="extractor:extract?location=WCSData"/>
            <log message="${id} WCSIngest: Finished Extraction"/>
            <log message="${id} WCSIngest: AbsolutePath - ${header.CamelFileAbsolutePath}"
                 loggingLevel="WARN"/>

            <threads poolSize="3" maxPoolSize="5">

                <!--If there is an issue with staging large archives we can can 'slow' down the route with delay below. -->
                <delay>
                    <constant>1000</constant>
                </delay>

                <transform>
                    <simple>deployment_manifest.xml</simple>
                </transform>
                <to uri="reader:file?type=text"/>
                <log message="${id} WCSIngest: Body - ${body}" loggingLevel="WARN"/>
                <to uri="validator:file:Input/schemas/Unified_WCSDeploymentManifest.xsd"/>

                <!-- Add Schematron validation. -->
                <!-- <to uri="schematron:///home/davisda/Projects/sidora/sem/apache-servicemix-5.4.0/Input/schemas/Unified_WCSDeploymentManifest.sch"/> -->
                <!-- <to uri="schematron:///opt/sidora/smx/Input/schemas/Unified_WCSDeploymentManifest.sch"/>
                <log message="${id} WCSIngest: Schematron Validation Status - ${header.CamelSchematronValidationStatus}"
                     loggingLevel="INFO"/> -->

                <!-- Add image count and filename check. -->
                <setHeader headerName="ManifestXML">
                    <simple>
                        ${body}
                    </simple>
                </setHeader>
                <to uri="direct:validatePackage"/>

                <log message="${id} WCSIngest: Ingest dry-run flag - {{si.ct.wcs.dryrun}}"
                     loggingLevel="DEBUG"
                     logName="edu.si.wcsingest"/>

                <choice>
                    <when>
                        <simple>{{si.ct.wcs.dryrun}} == true</simple>
                        <log message="${id} WCSIngest: Ingestion dryrun mode is enabled.. skipping the actual ingestion process" loggingLevel="INFO"/>
                    </when>
                    <otherwise>
                        <!-- Process the deployment -->
                        <to uri="direct:processPackage"/>
                    </otherwise>
                </choice>
            </threads>

            <log message="${id} WCSIngest: Finished WCS processing!"
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSValidatePackage">
            <from uri="direct:validatePackage"/>
            <log message="${id} WCSIngest: Starting Package validation ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <setHeader headerName="ResourceFileType">
                <simple resultType="java.lang.String">
                    .JPG, .JPEG
                </simple>
            </setHeader>
            <setHeader headerName="ResourceCount">
                <xpath resultType="java.lang.Integer">
                    count(//ImageFileName)
                </xpath>
            </setHeader>
            <to uri="bean:deploymentPkgValidator?method=validateResourceCount"/>
            <!--  Keeping the validator result in the header to allow the rest of the validation to go through and raise exception at the end of route. -->
            <setHeader headerName="ValidateResourceCountResult">
                <simple resultType="java.lang.Integer">
                    ${body}
                </simple>
            </setHeader>

            <split>
                <xpath headerName="ManifestXML">
                    //ImageFileName/text()
                </xpath>
                <log message="${id} WCSIngest: Split Resource - ${body}"
                     loggingLevel="DEBUG"
                     logName="edu.si.wcsingest"/>
                <setHeader headerName="ImageFileName">
                    <simple resultType="java.lang.String">
                        ${body}
                    </simple>
                </setHeader>
                <setHeader headerName="ImageSequenceID">
                    <xpath logNamespaces="false" resultType="java.lang.String" headerName="ManifestXML">
                        //ImageFileName[text()=$ImageFileName]/parent::Image/parent::ImageSequence/ImageSequenceId/text()
                    </xpath>
                </setHeader>
                <log message="${id} WCSIngest: ImageSequenceID - ${header.ImageSequenceID}"
                     loggingLevel="DEBUG"
                     logName="edu.si.wcsingest"/>
                <!-- This throws a java.io.FileNotFoundException if the file does not exist. -->
                <to uri="reader:file"/>
                <log message="${id} WCSIngest: CamelSplitSize - ${property.CamelSplitSize} CamelSplitIndex - ${property.CamelSplitIndex}"
                     loggingLevel="DEBUG"
                     logName="edu.si.wcsingest"/>
                <!-- We should check the image count to see if it exactly matches the image file count. -->
            </split>

            <!-- This throws custom exception when the resource count validator was unsuccessful. -->
            <choice>
                <when>
                    <simple>${header.ValidateResourceCountResult} == '0'</simple>
                    <throwException exceptionType="edu.si.services.beans.cameratrap.DeploymentPackageException"
                                    message="The resource counts do not match - extra resource(s) found in the file system than manifest"/>
                </when>
                <when>
                    <simple>${header.ValidateResourceCountResult} == '-1'</simple>
                    <throwException exceptionType="edu.si.services.beans.cameratrap.DeploymentPackageException"
                                    message="The resource counts do not match - less resource(s) found in the file system than manifest"/>
                </when>
            </choice>

            <log message="${id} WCSIngest: Finished Package validation."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessPackage">
            <from uri="direct:processPackage"/>
            <log message="${id} WCSIngest: Starting Package processing..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <to uri="direct:processParents"/>
            <to uri="direct:processSite"/>

            <log message="${id} WCSIngest: Finished Package processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessSite">
            <from uri="direct:processSite"/>
            <log message="${id} WCSIngest: Starting Site processing... "
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Stash the parent PID which could be a Plot or a Sub-project. -->
            <setHeader headerName="ParentPID">
                <simple>
                    ${header.CamelFedoraPid}
                </simple>
            </setHeader>
            <setHeader headerName="CamelFedoraLabel">
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    //CameraSiteName
                </xpath>
            </setHeader>
            <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}"/>
            <log message="${id} WCSIngest: Add Relation: Parent PID - ${header.ParentPID} Child PID - ${header.CamelFedoraPid}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:hasConcept?parentPid=${header.ParentPID}&amp;childPid=${header.CamelFedoraPid}"/>
            <log message="${id} WCSIngest: Add Relation: Status - ${header.CamelFedoraStatus}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <multicast>
                <to uri="direct:addManifestDataStream"/>
                <to uri="direct:addFGDCDataStream"/>
                <to uri="direct:processResources"/>
            </multicast>

            <log message="${id} WCSIngest: Finished Site processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddManifestDataStream">
            <from uri="direct:addManifestDataStream"/>
            <log message="${id} WCSIngest: Starting Manifest processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <to uri="fedora:addDatastream?name=MANIFEST&amp;type=text/xml&amp;group=M&amp;dsLabel=MANIFEST"/>

            <log message="${id} WCSIngest: Finished Manifest processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddFGDCDataStream">
            <from uri="direct:addFGDCDataStream"/>
            <log message="${id} WCSIngest: Starting FGDC processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <to uri="xslt:file:Input/xslt/ManifestDeployment.xsl?saxon=true"/>
            <to uri="fedora:addDatastream?name=FGDC&amp;type=text/xml&amp;group=M&amp;dsLabel=FGDC%20Record"/>

            <log message="${id} WCSIngest: Finished FGDC processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessResources">
            <from uri="direct:processResources"/>
            <log message="${id} WCSIngest: Starting resource processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <setHeader headerName="SitePID">
                <simple>
                    ${header.CamelFedoraPid}
                </simple>
            </setHeader>
            <setHeader headerName="ImageCount">
                <xpath resultType="java.lang.String">
                    count(//ImageFileName)
                </xpath>
            </setHeader>
            <log message="${id} WCSIngest: Site PID - ${header.SitePID}  Image Count - ${header.ImageCount}"
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- There may be several kinds of resources with zero or more instances. -->
            <split strategyRef="pidAggregator">
                <xpath headerName="ManifestXML">
                    //ImageFileName/text()
                </xpath>
                <log message="${id} WCSIngest: Split Resource - ${body}"
                     loggingLevel="INFO"
                     logName="edu.si.wcsingest"/>
                <setBody>
                    <simple resultType="java.lang.String">
                        ${body}
                    </simple>
                </setBody>
                <to uri="direct:addImageResource"/>
                <log message="${id} WCSIngest: Created Image Resource - ${body}."
                     loggingLevel="DEBUG"
                     logName="edu.si.wcsingest"/>
                <log message="${id} WCSIngest: CamelSplitSize - ${property.CamelSplitSize} CamelSplitIndex - ${property.CamelSplitIndex}"
                     loggingLevel="INFO"
                     logName="edu.si.wcsingest"/>
            </split>

            <!-- There is only one observation resource of each kind and all the observations are aggregated. -->
            <!-- Researcher is required for WCS. -->
            <to uri="direct:addResearcherObservationResource"/>
            <!-- Volunteer is optional for WCS. -->
            <filter>
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    boolean(//VolunteerIdentifications/Identification/IUCNId/text()[1])
                </xpath>
                <to uri="direct:addVolunteerObservationResource"/>
            </filter>
            <!-- Image is required for WCS. -->
            <to uri="direct:addImageObservationResource"/>

            <!-- Add the RELS-EXT datastream in one operation to gain about 70% of the total efficiency. -->
            <to uri="velocity:file:Input/templates/CTSiteTemplate.vsl"/>
            <recipientList>
                <simple>
                    fedora:addDatastream?pid=${header.SitePID}&amp;name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false
                </simple>
            </recipientList>

            <log message="${id} WCSIngest: Finished resource processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddImageResource" trace="true">
            <from uri="direct:addImageResource"/>
            <log message="${id} WCSIngest: Started Image processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!--
            <setHeader headerName="ImageID">
                <xpath logNamespaces="false" resultType="java.lang.String" headerName="ManifestXML">
                    //ImageFileName[text()=$in:body]/parent::Image/ImageId/text()
                </xpath>
            </setHeader>
            -->
            <!-- Spelled this way for the MODS XSLT parameter. -->
            <setHeader headerName="imageid">
                <xpath logNamespaces="false" resultType="java.lang.String" headerName="ManifestXML">
                    //ImageFileName[text()=$in:body]/parent::Image/ImageId/text()
                </xpath>
            </setHeader>

            <!-- Image Sequence ID used in MODS datastream. -->
            <!--
            <setHeader headerName="ImageSequenceID">
                <xpath logNamespaces="false" resultType="java.lang.String" headerName="ManifestXML">
                    //ImageFileName[text()=$in:body]/parent::Image/parent::ImageSequence/ImageSequenceId/text()
                </xpath>
            </setHeader>
            -->
            <setHeader headerName="ImageSequenceIndex">
                <xpath logNamespaces="false" resultType="java.lang.Integer" headerName="ManifestXML">
                    //ImageFileName[text()=$in:body]/parent::Image/ImageOrder/text()
                </xpath>
            </setHeader>
            <choice>
                <when>
                    <simple>
                        ${header.ImageSequenceIndex} == ''
                    </simple>
                    <!-- Stop processing this deployment. -->
                    <log message="${id} WCSIngest: Image sequence does not exist."
                         loggingLevel="WARN"
                         logName="edu.si.wcsingest"/>
                    <throwException ref="noImageSequence"/>
                </when>
            </choice>

            <!--
            <setHeader headerName="ImageSequenceCount">
                <xpath logNamespaces="false" resultType="java.lang.Integer" headerName="ManifestXML">
                    count(//Image[../ImageSequenceId=$ImageSequenceID])
                </xpath>
            </setHeader>
            <log message="${id} WCSIngest: Label - ${body}  ImageID - ${header.imageid}  ImageSequenceIndex - ${header.ImageSequenceIndex}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <log message="${id} WCSIngest: Label - ${body}  ImageSequenceID - ${header.ImageSequenceID}  ImageSequenceCount - ${header.ImageSequenceCount}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            -->
            <to uri="reader:file"/>
            <recipientList>
                <simple>
                    fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}&amp;label=${header.imageid}
                </simple>
            </recipientList>
            <recipientList>
                <simple>
                    fedora:addDatastream?name=OBJ&amp;type=image/jpeg&amp;group=M&amp;dsLabel=${header.imageid}&amp;versionable=false
                </simple>
            </recipientList>
            <!--<multicast>-->
            <to uri="direct:createThumbnail"/>
            <!-- <to uri="direct:createArchivalImage" /> -->
            <!-- We may also want a DISPLAY datastream. -->
            <!--</multicast>-->
            <to uri="velocity:file:Input/templates/CTImageResourceTemplate.vsl"/>
            <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>
            <to uri="direct:addFITSDataStream"/>
            <to uri="direct:addMODSDataStream"/>

            <!-- The current route only handles JPG. -->
            <choice>
                <!-- If the image is a JPEG? Do nothing. -->
                <when>
                    <simple>
                        ${header.dsMIME} == 'image/jpg' || ${header.dsMIME} == 'image/jpeg' || ${header.dsMIME} == 'image/jpe'
                    </simple>
                    <log message="${id} Derivatives: Found JPEG."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <!-- Future: Make a JPEG2000 archival image and store it in the MASTER datastream. -->
                </when>
                <!-- Just warn for now. -->
                <otherwise>
                    <log message="${id} Derivatives: Found non-JPEG Image."
                         loggingLevel="WARN"
                         logName="edu.si.wcsingest"/>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished Image processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddResearcherObservationResource">
            <from uri="direct:addResearcherObservationResource"/>
            <log message="${id} WCSIngest: Starting Researcher Observation data processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- For Researcher observations. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}&amp;label=Researcher%20Observations"/>
            <log message="Research Observation Resource PID - ${header.CamelFedoraPid}"
                    loggingLevel="DEBUG"
                    logName="edu.si.wcsingest"/>
            <setHeader headerName="PIDAggregation">
                <simple>${header.PIDAggregation},${header.CamelFedoraPid}</simple>
            </setHeader>
            <log message="Aggregation: ${header.PIDAggregation}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="xslt:file:Input/xslt/ResearcherObservation.xsl"/>
            <log message="Researcher Observations - \n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <transform>
                <xpath>
                    //researcher/text()
                </xpath>
            </transform>
            <convertBodyTo type="java.lang.String"/>
            <recipientList>
                <simple>
                    fedora:addDatastream?name=OBJ&amp;type=text/csv&amp;group=M&amp;dsLabel=Researcher%20Observations&amp;versionable=false"
                </simple>
            </recipientList>
            <to uri="fedora:addDatastream?name=CSV&amp;type=text/csv&amp;group=M&amp;dsLabel=CSV&amp;versionable=false"/>
            <to uri="velocity:file:Input/templates/CTDatasetResourceTemplate.vsl"/>
            <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>

            <!-- This is a quick fix but it is not elegant to fetch the manifest again to transform it. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="xslt:file:Input/xslt/ManifestResearcherObservation.xsl?saxon=true"/>
            <log message="${id} WCSIngest: FGDC XML -\n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:addDatastream?name=FGDC&amp;type=text/xml&amp;group=X&amp;dsLabel=FGDC%20Record"/>

            <log message="${id} WCSIngest: Finished Researcher Observation data processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddVolunteerObservationResource">
            <from uri="direct:addVolunteerObservationResource"/>
            <log message="${id} WCSIngest: Starting Volunteer Observation data processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- For Volunteer observations. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}&amp;label=Volunteer%20Observations"/>
            <log message="Volunteer Observation Resource PID - ${header.CamelFedoraPid}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <setHeader headerName="PIDAggregation">
                <simple>${header.PIDAggregation},${header.CamelFedoraPid}</simple>
            </setHeader>
            <log message="Aggregation: ${header.PIDAggregation}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="xslt:file:Input/xslt/VolunteerObservation.xsl"/>
            <log message="Volunteer Observations - \n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <transform>
                <xpath>
                    //volunteer/text()
                </xpath>
            </transform>
            <convertBodyTo type="java.lang.String"/>
            <recipientList>
                <simple>
                    fedora:addDatastream?name=OBJ&amp;type=text/csv&amp;group=M&amp;dsLabel=Volunteer%20Observations&amp;versionable=false"
                </simple>
            </recipientList>
            <to uri="fedora:addDatastream?name=CSV&amp;type=text/csv&amp;group=M&amp;dsLabel=CSV&amp;versionable=false"/>
            <to uri="velocity:file:Input/templates/CTDatasetResourceTemplate.vsl" />
            <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>

            <!-- This is a quick fix but it is not elegant to fetch the manifest again to transform it. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="xslt:file:Input/xslt/ManifestVolunteerObservation.xsl?saxon=true"/>
            <log message="${id} WCSIngest: FGDC XML -\n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:addDatastream?name=FGDC&amp;type=text/xml&amp;group=X&amp;dsLabel=FGDC%20Record"/>

            <log message="${id} WCSIngest: Finished Volunteer Observation data processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddImageObservationResource">
            <from uri="direct:addImageObservationResource"/>
            <log message="${id} WCSIngest: Starting Image Observation data processing ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- For Image observations. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}&amp;label=Image%20Observations"/>
            <log message="Image Observation Resource PID - ${header.CamelFedoraPid}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <setHeader headerName="PIDAggregation">
                <simple>${header.PIDAggregation},${header.CamelFedoraPid}</simple>
            </setHeader>
            <log message="Aggregation: ${header.PIDAggregation}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="xslt:file:Input/xslt/ImageObservation.xsl"/>
            <log message="Image Observations - \n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <transform>
                <xpath>
                    //image/text()
                </xpath>
            </transform>
            <convertBodyTo type="java.lang.String"/>
            <recipientList>
                <simple>
                    fedora:addDatastream?name=OBJ&amp;type=text/csv&amp;group=M&amp;dsLabel=Image%20Observations&amp;versionable=false"
                </simple>
            </recipientList>
            <to uri="fedora:addDatastream?name=CSV&amp;type=text/csv&amp;group=M&amp;dsLabel=CSV&amp;versionable=false"/>
            <to uri="velocity:file:Input/templates/CTDatasetResourceTemplate.vsl"/>
            <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>

            <!-- This is a quick fix but it is not elegant to fetch the manifest again to transform it. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="xslt:file:Input/xslt/ManifestImageObservation.xsl?saxon=true"/>
            <log message="${id} WCSIngest: FGDC XML -\n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:addDatastream?name=FGDC&amp;type=text/xml&amp;group=X&amp;dsLabel=FGDC%20Record"/>

            <log message="${id} WCSIngest: Finished Image Observation data processing."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddFITSDataStream">
            <from uri="direct:addFITSDataStream"/>
            <log message="${id} WCSIngest: Started processing FITS ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <recipientList>
                <simple>
                    exec:fits?args=-i ${header.CamelFileAbsolutePath}
                </simple>
            </recipientList>
            <choice>
                <!-- If FITS processing succeeded? Store a FITS datastream on the FDO. -->
                <when>
                    <simple>
                        ${header.CamelExecExitValue} == 0
                    </simple>
                    <setHeader headerName="dsMIME">
                        <xpath resultType="java.lang.String">/fits:fits/fits:identification/fits:identity[1]/@mimetype</xpath>
                        <log message="${id} WCSIngest: FITS MIME: ${header.FITSmimeType}"
                             loggingLevel="DEBUG"
                             logName="edu.si.wcsingest"/>
                    </setHeader>
                    <log message="${id} WCSIngest: Exec FITS. BODY: ${body}"
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <to uri="fedora:addDatastream?name=FITS&amp;type=text/xml&amp;dsLabel=FITS%20Generated%20Image%20Metadata&amp;group=X&amp;versionable=false"/>
                </when>
                <otherwise>
                    <log message="${id} WCSIngest: FITS processing failed. PID: ${header.CamelFedoraPid}  Error Code: ${header.CamelExecExitValue}"
                         loggingLevel="ERROR"/>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished processing FITS."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSAddMODSDataStream">
            <from uri="direct:addMODSDataStream"/>
            <log message="${id} WCSIngest: Started processing MODS ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Get the FITS XML and use XPATH to get the created date from it. -->
            <!--
            <setHeader headerName="FITSCreatedDate">
                <xpath logNamespaces="false" resultType="java.lang.String">
                    //fits:fileinfo/fits:created[@toolname="Exiftool"]
                </xpath>
            </setHeader>
            <log message="MODS FITS Created Date - ${header.FITSCreatedDate}"/>
            -->

            <!-- TODO - The Image ID has .JPG appended and we may want to make this consistent. -->
            <!-- Add a MODS datastream for concept metadata. -->
            <setBody>
                <simple>
                    ${header.ManifestXML}
                </simple>
            </setBody>
            <to uri="xslt:file:Input/xslt/ManifestImage.xsl?saxon=true"/>
            <log message="${id} WCSIngest: MODS XML -\n${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:addDatastream?name=MODS&amp;type=text/xml&amp;group=X&amp;dsLabel=MODS%20Record"/>

            <log message="${id} WCSIngest: Finished processing MODS."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSCreateThumbnailImage">
            <from uri="direct:createThumbnail"/>
            <log message="${id} WCSIngest: Started creating thumbnail ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!--
            <onException>
                <exception>javax.imageio.IIOException</exception>
                <redeliveryPolicy maximumRedeliveries="0"/>
                <continued>
                    <constant>
                        true
                    </constant>
                </continued>
                <stop/>
            </onException>
            -->

            <doTry>
                <to uri="thumbnailator:image?keepRatio=true&amp;size=(200,150)"/>
                <to uri="fedora:addDatastream?name=TN&amp;type=image/jpeg&amp;group=M&amp;dsLabel=Thumbnail&amp;versionable=false"/>
                <doCatch>
                    <exception>javax.imageio.IIOException</exception>
                    <log message="${id} WCSIngest: Cannot create thumbnail image corrupted."
                         loggingLevel="WARN"
                         logName="edu.si.wcsingest"/>
                </doCatch>
            </doTry>

            <log message="${id} WCSIngest: Finished creating thumbnail."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSCreateArchivalImage">
            <from uri="direct:createArchivalImage"/>
            <log message="${id} WCSIngest: Started creating archival image ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!--
                NOTE: This is planned to be a JPEG 2000 but we are not doing it yet.
            <to uri="thumbnailator:image?quality=80%&amp;size=(2048,1536)"/>
            <to uri="file://WCSOutput/resample"/>
            -->

            <log message="${id} WCSIngest: Finished creating archival image."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessParents">
            <from uri="direct:processParents"/>
            <log message="${id} WCSIngest: Started processing parents ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <setBody>
                <simple>
                    {{si.ct.root}}
                </simple>
            </setBody>
            <!-- Check if the root exists, else quit. -->
            <to uri="direct:findObjectByPIDPredicate"/>
            <choice>
                <when>
                    <simple>
                        ${body} == 'true'
                    </simple>
                    <log message="${id} WCSIngest: Root object exists."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <setHeader headerName="CamelFedoraPid">
                        <simple>
                            {{si.ct.root}}
                        </simple>
                    </setHeader>
                    <!-- Add the parents if needed. -->
                    <to uri="direct:processProject"/>
                    <!-- Subproject is optional for WCS. -->
                    <filter>
                        <xpath resultType="java.lang.String" headerName="ManifestXML">
                            boolean(//SubProjectName/text()[1])
                        </xpath>
                        <to uri="direct:processSubproject"/>
                    </filter>
                    <!-- Plot is optional. -->
                    <filter>
                        <xpath resultType="java.lang.String" headerName="ManifestXML">
                            boolean(//PlotName/text()[1])
                        </xpath>
                        <to uri="direct:processPlot"/>
                    </filter>
                    <!-- Clean up parent ID(s) from the static storage to unlock the wait for follow-on ingestions with same parent identifiers. -->
                    <to uri="bean:cameraTrapStaticStore?method=removeParentIdsByDeploymentId(${header.CamelFileParent})"/>
                </when>
                <otherwise>
                    <!-- Stop processing this deployment. -->
                    <log message="${id} WCSIngest: Root object does not exist."
                         loggingLevel="WARN"
                         logName="edu.si.wcsingest"/>
                    <throwException ref="noRootObject"/>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished processing parents."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSFindObject" errorHandlerRef="noErrorHandler">
            <from uri="direct:findObject"/>
            <log message="${id} WCSIngest: Started find object ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
            <log message="${id} WCSIngest: Find: Label - ${body}  Parent - ${header.CamelFedoraPid}"
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!--
            <transform>
                <simple>
                    ${body.replaceAll("'", "\'")}
                </simple>
            </transform>
            -->
            <!-- There is a bug in Camel that causes replaceAll to fail so I am using Groovy for now.  We
                 will want to remove this so we don't have to include the Groovy feature just for this.
                 The bug is fixed in newer versions of Camel.
            -->
            <transform>
                <groovy>
                    request.body.replace("'", "\\'")
                </groovy>
            </transform>

            <log message="Replace body - ${body}" loggingLevel="WARN"/>

            <setBody>
                <!-- The query needs to also check directly along the parent axis since names are not unique. -->
                <simple>
                    SELECT ?o FROM &lt;#ri&gt;
                    WHERE
                    {
                        ?o &lt;fedora-model:label&gt; '${body}' .
                        &lt;info:fedora/${header.CamelFedoraPid}&gt; &lt;info:fedora/fedora-system:def/relations-external#hasConcept&gt; ?o .
                    }
                </simple>
            </setBody>
            <log message="${id} WCSIngest: Project Parent - ${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:search?lang=sparql&amp;type=tuples&amp;format=sparql"/>
            <log message="${id} WCSIngest: Query Result - ${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <!-- Count the results. If greater than one warn about duplicate parents. -->
            <choice>
                <when>
                    <xpath>
                        count(/ri:sparql/ri:results/ri:result) > 1
                    </xpath>
                    <log message="${id} WCSIngest: Warning - Duplicate parents."
                         loggingLevel="WARN"
                         logName="edu.si.wcsingest"/>
                </when>
            </choice>
            <setBody>
                <xpath logNamespaces="false" resultType="java.lang.String">
                    substring-after(/ri:sparql/ri:results/ri:result[1]/ri:o/@uri,'/')
                </xpath>
            </setBody>

            <choice>
                <when>
                    <simple>
                        ${body} == ''
                    </simple>
                    <!-- Throw custom exception to trigger re-try attempts on search -->
                    <throwException exceptionType="edu.si.services.fedorarepo.FedoraObjectNotFoundException"
                                    message="The fedora object not found"/>
                </when>
            </choice>
            <log message="${id} WCSIngest: Finished find object."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSFindObjectByPIDPredicate" errorHandlerRef="noErrorHandler">
            <from uri="direct:findObjectByPIDPredicate"/>
            <log message="${id} WCSIngest: Started find object by PID ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Return true in the body if the object exists for the PID, false if not. -->
            <setBody>
                <simple>
                    ASK FROM &lt;#ri&gt;
                    {
                        &lt;info:fedora/${body}&gt; ?p ?o .
                    }
                </simple>
            </setBody>
            <log message="${id} WCSIngest: Find Query - ${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="fedora:search?lang=sparql&amp;type=tuples&amp;format=sparql"/>
            <log message="${id} WCSIngest: Find Query Result - ${body}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <setBody>
                <xpath logNamespaces="false" resultType="java.lang.String">
                    /ri:sparql/ri:results/ri:result[1]/ri:k0/text()
                </xpath>
            </setBody>
            <choice>
                <when>
                    <simple>
                        ${body} == false
                    </simple>
                    <!-- Throw custom exception to trigger re-try attempts on search -->
                    <throwException exceptionType="edu.si.services.fedorarepo.FedoraObjectNotFoundException"
                                    message="The fedora object not found"/>
                </when>
            </choice>
            <log message="Find Object By PID - ${body}."
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>

            <log message="${id} WCSIngest: Finished find object by PID."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessPlot">
            <from uri="direct:processPlot"/>
            <log message="${id} WCSIngest: Started processing plot ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Look for the plot (its optional). -->
            <setBody>
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    concat(//SubProjectName/text(), ':', //PlotName/text())
                </xpath>
            </setBody>
            <setHeader headerName="CamelFedoraLabel">
                <simple>
                    ${body}
                </simple>
            </setHeader>
            <!-- stash parent identifier to be used for the in-flight parent process checks. -->
            <setHeader headerName="DeploymentParentId">
                <simple>
                    ${body}
                </simple>
            </setHeader>
            <log message="${id} WCSIngest: Plot: Label - ${body}  Parent PID - ${header.CamelFedoraPid}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="direct:findObject" />
            <choice>
                <when>
                    <simple>
                        ${body} == ''
                    </simple>
                    <log message="${id} WCSIngest: Plot does not exist"
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <!-- Stash the sub-project PID. -->
                    <setHeader headerName="ParentPID">
                        <simple>
                            ${header.CamelFedoraPid}
                        </simple>
                    </setHeader>
                    <!-- Create CT plot and add it as a sub-concept of the sub-project -->
                    <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}"/>
                    <!-- Add a minimal RELS-EXT to the project -->
                    <to uri="velocity:file:Input/templates/CTPlotTemplate.vsl"/>
                    <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>
                    <!-- Add Relation from parent CT sub-project to the child CT plot -->
                    <to uri="fedora:hasConcept?parentPid=${header.ParentPID}&amp;childPid=${header.CamelFedoraPid}"/>
                    <!-- Add an EAC-CPF datastream for concept metadata. -->
                    <setBody>
                        <simple>
                            ${header.ManifestXML}
                        </simple>
                    </setBody>
                    <to uri="xslt:file:Input/xslt/ManifestPlot.xsl?saxon=true"/>
                    <to uri="fedora:addDatastream?name=FGDC-CTPlot&amp;type=text/xml&amp;group=X&amp;dsLabel=FGDC-CTPlot%20Record&amp;versionable=true"/>
                </when>
                <otherwise>
                    <!-- Return the existing plot as the current object. -->
                    <log message="${id} WCSIngest: Plot already exists - ${body}."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <setHeader headerName="CamelFedoraPid">
                        <simple>
                            ${body}
                        </simple>
                    </setHeader>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished processing plot."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessSubproject">
            <from uri="direct:processSubproject"/>
            <log message="${id} WCSIngest: Started processing subproject ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Look for the sub-project. -->
            <setBody>
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    //SubProjectName
                </xpath>
            </setBody>
            <setHeader headerName="CamelFedoraLabel">
                <simple>
                    ${body}
                </simple>
            </setHeader>
            <!-- stash subproject identifier to be used for the in-flight parent process checks. -->
            <setHeader headerName="DeploymentParentId">
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    //SubProjectId
                </xpath>
            </setHeader>
            <log message="${id} WCSIngest: Sub-project: Label - ${body}  Parent PID - ${header.CamelFedoraPid}"
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="direct:findObject" />
            <choice>
                <when>
                    <simple>
                        ${body} == ''
                    </simple>
                    <log message="${id} WCSIngest: Sub-project does not exist"
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <!-- Stash the Project PID. -->
                    <setHeader headerName="ParentPID">
                        <simple>
                            ${header.CamelFedoraPid}
                        </simple>
                    </setHeader>
                    <!-- Create CT sub-project and add it as a sub-concept of the project. -->
                    <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}"/>
                    <!-- Add a minimal RELS-EXT to the sub-project -->
                    <to uri="velocity:file:Input/templates/CTPlotTemplate.vsl"/>
                    <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>
                    <!-- Add Relation from parent CT project to the child CT sub-project -->
                    <to uri="fedora:hasConcept?parentPid=${header.ParentPID}&amp;childPid=${header.CamelFedoraPid}"/>
                    <!-- Add an EAC-CPF datastream for concept metadata. -->
                    <setBody>
                        <simple>
                            ${header.ManifestXML}
                        </simple>
                    </setBody>
                    <to uri="xslt:file:Input/xslt/ManifestSubproject.xsl?saxon=true"/>
                    <to uri="fedora:addDatastream?name=FGDC-Research&amp;type=text/xml&amp;group=X&amp;dsLabel=FGDC-Research%20Record&amp;versionable=true"/>
                </when>
                <otherwise>
                    <!-- Return the existing sub-project as the current object. -->
                    <log message="${id} WCSIngest: Sub-project already exists - ${body}."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <setHeader headerName="CamelFedoraPid">
                        <simple>
                            ${body}
                        </simple>
                    </setHeader>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished processing subproject."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

        <route id="WCSProcessProject">
            <from uri="direct:processProject"/>
            <log message="${id} WCSIngest: Started processing project ..."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>

            <!-- Look for the project. -->
            <setBody>
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    //ProjectName
                </xpath>
            </setBody>
            <setHeader headerName="CamelFedoraLabel">
                <simple>
                    ${body}
                </simple>
            </setHeader>
            <!-- stash project identifier to be used for the in-flight parent process checks. -->
            <setHeader headerName="DeploymentParentId">
                <xpath resultType="java.lang.String" headerName="ManifestXML">
                    //ProjectId
                </xpath>
            </setHeader>
            <log message="${id} WCSIngest: Label - ${body}  Parent PID - ${header.CamelFedoraPid}."
                 loggingLevel="DEBUG"
                 logName="edu.si.wcsingest"/>
            <to uri="direct:findObject"/>
            <choice>
                <when>
                    <simple>
                        ${body} == ''
                    </simple>
                    <log message="${id} WCSIngest: Project does not exist."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <!-- Create CT project and add it as a sub-concept of the Camera Trap root object. -->
                    <to uri="fedora:create?pid=null&amp;owner={{si.ct.owner}}&amp;namespace={{si.ct.namespace}}"/>
                    <!-- Add a minimal RELS-EXT to the project. -->
                    <to uri="velocity:file:Input/templates/CTProjectTemplate.vsl"/>
                    <to uri="fedora:addDatastream?name=RELS-EXT&amp;group=X&amp;dsLabel=RDF%20Statements%20about%20this%20object&amp;versionable=false"/>
                    <!-- Add Relation from parent CT root object to the child CT project. -->
                    <to uri="fedora:hasConcept?parentPid={{si.ct.root}}&amp;childPid=${header.CamelFedoraPid}"/>
                    <!-- Add an EAC-CPF datastream for concept metadata. -->
                    <setBody>
                        <simple>
                            ${header.ManifestXML}
                        </simple>
                    </setBody>
                    <to uri="xslt:file:Input/xslt/ManifestProject.xsl?saxon=true"/>
                    <to uri="fedora:addDatastream?name=EAC-CPF&amp;type=text/xml&amp;group=X&amp;dsLabel=EAC-CPF&amp;versionable=true"/>
                </when>
                <otherwise>
                    <!-- Return the existing project as the current object. -->
                    <log message="${id} WCSIngest: Project already exists - ${body}."
                         loggingLevel="DEBUG"
                         logName="edu.si.wcsingest"/>
                    <setHeader headerName="CamelFedoraPid">
                        <simple>
                            ${body}
                        </simple>
                    </setHeader>
                </otherwise>
            </choice>

            <log message="${id} WCSIngest: Finished processing project."
                 loggingLevel="INFO"
                 logName="edu.si.wcsingest"/>
        </route>

    </camelContext>
</blueprint>